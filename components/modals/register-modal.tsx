import LoginOrRegister from "@/components/login-or-register";

const LoginOrRegisterModal = () => {
  const modalContent = <LoginOrRegister />;

  return modalContent;
};

export default LoginOrRegisterModal;
